# GPU-Accelerated Dependencies
# Install these AFTER requirements.txt for NVIDIA GPU support
# Requires CUDA 11.8 or later

# Replace CPU-only PyTorch with CUDA version
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.2.0+cu118
torchvision>=0.17.0+cu118

# TensorRT for optimized inference (optional but recommended)
# Note: Requires separate TensorRT installation from NVIDIA
# tensorrt>=8.6.0

# NVIDIA GPU utilities
nvidia-ml-py>=12.535.0
